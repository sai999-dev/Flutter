# Quick Start - Run Flutter App

## ✅ Setup Complete!

Both backend and frontend are ready to run.

---

## 🚀 Run the App

### Step 1: Navigate to Frontend

```bash
cd flutter-frontend
```

### Step 2: Run the App

```bash
flutter run
```

Or specify a device:

```bash
# Android
flutter run -d android

# iOS (macOS only)
flutter run -d ios

# Web
flutter run -d web

# Windows
flutter run -d windows
```

---

## 📱 Available Devices

Check available devices:
```bash
flutter devices
```

---

## ⚡ One-Liner Commands

### Run on Default Device
```bash
cd flutter-frontend && flutter run
```

### Run on Web (Fastest for Testing)
```bash
cd flutter-frontend && flutter run -d web
```

### Run on Android
```bash
cd flutter-frontend && flutter run -d android
```

---

## 🔧 If You Get Errors

### Clean and Rebuild:
```bash
cd flutter-frontend
flutter clean
flutter pub get
flutter run
```

### Verify Backend Package:
```bash
cd flutter-backend
flutter pub get
```

---

## ✅ You're Ready!

The app is set up and ready to run. Use `flutter run` from the `flutter-frontend/` folder!

